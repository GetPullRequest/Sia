export type siteConfig = typeof siteConfig;

export const siteConfig = {
  name: 'Sia (getpullrequest)',
  description:
    'AI Developer Assistant that automates small, well-defined coding tasks and creates ready-to-review Pull Requests (PRs) while the human developer is offline or sleeping.',
  icon: '/favicon.svg',
};
